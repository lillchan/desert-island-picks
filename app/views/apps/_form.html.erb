<% if @app.errors.any? %>
	<h2>Oh noes, there were errors saving a new app!</h2>
	<ul>
		<% @app.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
		<% end %>
	</ul>
<% end %>

<%= form_for @app do |f| %>
	<%= f.label :name %>
	<%= f.text_field :name %>
	<br>
	<%= f.submit %>
<% end %>